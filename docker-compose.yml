services:
  app:
    container_name: esratansuk-personal-website
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "44397:3000"
    environment:
      - NODE_ENV=production
    restart: always
    dns:
      - 8.8.8.8

