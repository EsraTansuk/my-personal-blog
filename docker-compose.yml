version: "3.8"

services:
  Foxy.PersonalBlog:
    image: foxy-personal-blog
    container_name: foxy-personal-blog-container
    ports:
      - "3000:3000"
    restart: always
    build:
      context: .
      dockerfile: Dockerfile
